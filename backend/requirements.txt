pandas
numpy
psycopg2
openai
pgvector
pydantic
fastapi
uvicorn
python-dotenv
slowapi
# Imported at module load by backend/embedding.py (pulled in transitively by
# InferenceQuery -> the API). Pin to whatever you validate locally.
# CPU wheel for torch: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==6.0.1
torch==2.14.0
