# Base URL of the FastAPI backend (backend/api/main.py).
# Used by both server components and the browser, so it must be reachable
# from both. For local dev the default below is fine.
NEXT_PUBLIC_API_BASE=http://localhost:8000
